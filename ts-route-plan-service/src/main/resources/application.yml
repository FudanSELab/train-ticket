spring:
  cloud:
    nacos:
      discovery:
        server-addr: ${NACOS_ADDRS:127.0.0.1:8848}
  application:
    name: ts-route-plan-service
  datasource:
    url: jdbc:mysql://${ROUTE_PLAN_MYSQL_HOST:ts-route-plan-mysql}:${ROUTE_PLAN_MYSQL_PORT:3306}/${ROUTE_PLAN_MYSQL_DATABASE:ts-route-plan-mysql}?useSSL=false
    username: ${ROUTE_PLAN_MYSQL_USER:root}
    password: ${ROUTE_PLAN_MYSQL_PASSWORD:Abcd1234#}
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL5Dialect

# HTTP Server
server:
  port: 11178  # HTTP (Tomcat) port

swagger:
  controllerPackage: route.controller